# Inference-only requirements for `generate.py` (StyleGAN2-ADA).
#
# PyTorch note:
# - Updated to allow current torch versions.
# - For Docker/GPUs, install the wheel matching your CUDA/base image, e.g.:
#   - CPU:    pip install --index-url https://download.pytorch.org/whl/cpu torch
#   - CUDA:   pip install --index-url https://download.pytorch.org/whl/cu118 torch

torch>=2.0
numpy
Pillow
requests
click
scipy
fastapi
uvicorn
